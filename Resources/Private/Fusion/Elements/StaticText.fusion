prototype(Neos.Form:StaticText) < prototype(Wwwision.Form.FusionPreset:FormElement) {
    fieldContainer.field.tagName = 'p'
    # TODO htmlspecialchars text
    fieldContainer.field.content = ${element.properties.text}
}