prototype(Neos.Form:Password) < prototype(Wwwision.Form.FusionPreset:FormElement) {
    fieldContainer {
        field {
            tagName = 'input'
            attributes {
                type = 'password'
                name = ${elementName}
                value = ${elementValue}
            }
        }
    }
}