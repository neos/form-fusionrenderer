prototype(Neos.Form:Password) < prototype(Neos.Form.FusionRenderer:FormElement) {
    fieldContainer {
        field {
            tagName = 'input'
            attributes {
                type = 'password'
                name = ${elementName}
                value = ${elementValue}
            }
        }
    }
}