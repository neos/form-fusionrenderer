prototype(Neos.Form:MultiLineText) < prototype(Neos.Form.FusionRenderer:FormElement) {
    fieldContainer {
        field {
            tagName = 'textarea'
            attributes {
                name = ${elementName}
            }
            content = ${String.htmlSpecialChars(elementValue)}
        }
    }
}
