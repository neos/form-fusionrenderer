prototype(Neos.Form:DatePicker) < prototype(Wwwision.Form.FusionPreset:FormElement) {
    fieldContainer {
        field {
            tagName = 'input'
            attributes {
                type = 'date'
                name = ${elementName}
                value = ${elementValue}
            }
        }
    }
}
