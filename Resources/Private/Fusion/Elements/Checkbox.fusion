prototype(Neos.Form:Checkbox) < prototype(Wwwision.Form.FusionPreset:FormElement) {
    field.attributes.type = 'checkbox'
    field.attributes.checked = ${true}
    field.attributes.checked.@if.isSelected = ${FusionPreset.Validation.value(request, element.identifier)}
}
